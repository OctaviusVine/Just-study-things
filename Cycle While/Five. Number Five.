'''
New topic: "break, continue, else"

Task: smallest divisor
The input to the program is the number n > 1. I wrote a program that outputs its smallest divisor other than 1.
''''

n = int(input())
for i in range(2, n + 1):
    if n % i == 0:
        print(i)
        break
