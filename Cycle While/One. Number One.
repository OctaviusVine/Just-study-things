'''
This program just read the typed number and trying to find MAX and MIN figures from it and then prints it.
Version 1.
'''

n = int(input())
max1 = 0
min1 = 9
while n != 0:
    last_digit = n % 10
    if last_digit > max1:
        max1 = last_digit
    if last_digit < min1:
        min1 = last_digit
    n = n // 10
print("The maximum digit is", max1)
print("The minimum digit is", min1)

'''
Version 2.
'''

n = int(input())
n = str(n)
print("The maximum digit is", max(n))
print("The minimum digit is", min(n))

'''
That's the end!
'''
