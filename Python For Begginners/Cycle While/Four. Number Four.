'''
Given a natural number. I wrote a program that determines whether the sequence of its digits when viewed from right to left is ordered in non-decreasing order.
'''

n = int(input())
prelast = 0
last_digit = 0
flag = True
while n != 0:
    prelast = last_digit
    last_digit = n % 10
    if last_digit < prelast:
        flag = False
    n = n // 10
if flag == True:
    print("YES")
else:
    print("NO")
